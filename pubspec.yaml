name: NxT_BUS
description: A Company by Kaliyuga Adventure

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.15.1
  firebase_auth: ^4.9.0
  cloud_firestore: ^4.9.1
  firebase_storage: ^11.2.6
  firebase_messaging: ^14.6.7
  
  # State Management
  riverpod: ^2.4.0
  flutter_riverpod: ^2.4.0
  
  # UI & Navigation
  cupertino_icons: ^1.0.2
  google_fonts: ^5.1.0
  flutter_screenutil: ^5.8.4
  animations: ^2.0.7
  carousel_slider: ^4.2.1
  
  # Maps & Location
  google_maps_flutter: ^2.5.0
  geolocator: ^9.0.2
  geocoding: ^2.1.0
  
  # Payment
  razorpay_flutter: ^1.3.5
  
  # Utilities
  intl: ^0.18.1
  shared_preferences: ^2.2.0
  image_picker: ^1.0.2
  qr_flutter: ^4.1.0
  qr_code_scanner: ^1.0.1
  url_launcher: ^6.1.12
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
